using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tryGIThub : MonoBehaviour
{
    public float trysss;

    // Start is called before the first frame update
    void Start()
    {
        trysss = 3.5f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
